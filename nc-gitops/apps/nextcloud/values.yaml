# Keep Postgres at the top
postgresql:
  enabled: true
  auth:
    database: nctracex
    username: ncusr
    password: 'DSF29jha64'

# Most reliable way to force S3 in the official chart:
nextcloud:
  host: nc.tracexit.com
  # Use the standard objectStore block
  objectStore:
    enabled: true
    config:
      region: 'us-east-1'
      bucket: 'nextcloud'
      endpoint: 'minio.minio.svc.cluster.local'
      port: 9000
      use_ssl: false
      autocreate: true
      key: 'rfibb9ivKUwQA7AxwPVD'
      secret: 'p98m97807cch4KhuhjefjJjQhy4w8wV26OWs2v8E'

  configs:
    # REMOVED s3.config.php from here to stop the 'unique' error
    proxy.config.php: |-
      <?php
      $CONFIG = array (
        'trusted_proxies' => array('192.168.20.110'),
        'overwriteprotocol' => 'https',
      );
    oidc.config.php: |-
      <?php
      $CONFIG = array (
        'oidc_login_client_id' => 'nextcloud',
        'oidc_login_client_secret' => 'E4KdTMA0caH3UtAeILi5RMVm9gRz0hom',
        'oidc_login_provider_url' => 'https://sso-kc.tracexit.com/realms/NC',
        'oidc_login_auto_redirect' => false,
        'oidc_login_button_text' => 'Log in with Keycloak',
        'oidc_login_display_name_claim' => 'preferred_username',
        'oidc_login_use_id_token' => true,
      );