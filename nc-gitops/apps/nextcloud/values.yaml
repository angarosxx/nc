# 1. PostgreSQL - Top Level
postgresql:
  enabled: true
  auth:
    database: nctracex
    username: ncusr
    password: 'DSF29jha64'

# 2. Ingress - MUST BE TOP LEVEL (FLUSH LEFT)
ingress:
  enabled: true
  className: traefik
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.entrypoints: web
  hosts:
    - host: nc.tracexit.com
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: nextcloud
              port:
                number: 80

# 3. Nextcloud App Settings - Top Level
nextcloud:
  host: nc.tracexit.com
  objectStore:
    enabled: false 

  # Force the installation via Environment Variables
  extraEnv:
    - name: NEXTCLOUD_ADMIN_USER
      value: "admin"
    - name: NEXTCLOUD_ADMIN_PASSWORD
      value: "SetYourStrongPasswordHere"
    - name: OBJECTSTORE_S3_BUCKET
      value: "nextcloud"
    - name: OBJECTSTORE_S3_HOST
      value: "minio.minio.svc.cluster.local"
    - name: OBJECTSTORE_S3_PORT
      value: "9000"
    - name: OBJECTSTORE_S3_KEY
      value: "rfibb9ivKUwQA7AxwPVD"
    - name: OBJECTSTORE_S3_SECRET
      value: "p98m97807cch4KhuhjefjJjQhy4w8wV26OWs2v8E"
    - name: OBJECTSTORE_S3_REGION
      value: "us-east-1"
    - name: OBJECTSTORE_S3_SSL
      value: "false"
    - name: OBJECTSTORE_S3_USEPATH_STYLE
      value: "true"

  configs:
    00-primary-s3.config.php: |-
      <?php
      $CONFIG = array (
        'objectstore' => array(
          'class' => '\\OC\\Files\\ObjectStore\\S3',
          'arguments' => array(
            'bucket' => 'nextcloud',
            'autocreate' => true,
            'key'    => 'rfibb9ivKUwQA7AxwPVD',
            'secret' => 'p98m97807cch4KhuhjefjJjQhy4w8wV26OWs2v8E',
            'hostname' => 'minio.minio.svc.cluster.local',
            'port' => 9000,
            'use_ssl' => false,
            'region' => 'us-east-1',
            'use_path_style'=> true
          ),
        ),
      );
    proxy.config.php: |-
      <?php
      $CONFIG = array (
        'trusted_proxies' => array('192.168.20.110'),
        'overwriteprotocol' => 'https',
        'trusted_domains' => array('nc.tracexit.com'),
        'overwrite.cli.url' => 'https://nc.tracexit.com',
      );

# 4. Probes - Top Level
livenessProbe:
  enabled: false
readinessProbe:
  enabled: false