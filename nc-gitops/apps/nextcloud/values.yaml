postgresql:
  enabled: true
  auth:
    database: nctracex
    username: ncusr
    password: 'DSF29jha64'
livenessProbe:
  enabled: true
  initialDelaySeconds: 300  # Give it 5 minutes to figure itself out
  periodSeconds: 10
  failureThreshold: 10
readinessProbe:
  enabled: true
  initialDelaySeconds: 300
  periodSeconds: 10

nextcloud:
  host: nc.tracexit.com
  # Use the standard objectStore block but with path style forced
  objectStore:
    enabled: true
    config:
      region: 'us-east-1'
      bucket: 'nextcloud'
      endpoint: 'minio.minio.svc.cluster.local'
      port: 9000
      use_ssl: false
      autocreate: true
      key: 'BwwEY8pVJO2w11xwPRL5'
      secret: 'UqJrfP5Rv5w0qa7ZZpGIK4QuIGsUsZQGxlEvNuml'
      # Force Path Style for MinIO
      extraArgs: 
        use_path_style: true

  configs:
    proxy.config.php: |-
      <?php
      $CONFIG = array (
        'trusted_proxies' => array('192.168.20.110'),
        'overwriteprotocol' => 'https',
      );
    # This block manually fixes the S3 connection logic if the chart fails
    z-s3.config.php: |-
      <?php
      $CONFIG = array (
        'objectstore' => array(
          'class' => 'OC\\Files\\ObjectStore\\S3',
          'arguments' => array(
            'bucket' => 'nextcloud',
            'autocreate' => true,
            'key'    => 'rfibb9ivKUwQA7AxwPVD',
            'secret' => 'p98m97807cch4KhuhjefjJjQhy4w8wV26OWs2v8E',
            'hostname' => 'minio.minio.svc.cluster.local',
            'port' => 9000,
            'use_ssl' => false,
            'region' => 'us-east-1',
            'use_path_style'=> true
          ),
        ),
      );