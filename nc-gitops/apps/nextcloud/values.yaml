# 1. Disable the default internal SQLite
internalDatabase:
  enabled: false

# 2. Enable and configure the PostgreSQL sub-chart
postgresql:
  enabled: true
  auth:
    database: nctracex
    username: ncusr
    password: "DSF29jha64"
  primary:
    persistence:
      enabled: true
      storageClass: "local-path"
      size: 10Gi

# 2. Nextcloud Application Settings (MERGED)
nextcloud:
  host: nc.tracexit.com
  datadir: /var/www/html/data
  configs:
    proxy.config.php: |-
      <?php
      $CONFIG = array (
        'trusted_proxies' => array('192.168.20.110'), 
        'overwriteprotocol' => 'https',
      );
# This automatically installs the OIDC app on startup
  extraApps:
    oidc_login: https://github.com/pulsejet/nextcloud-oidc-login/releases/latest/download/oidc_login.tar.gz

  configs:
    # Add this to your existing configs block
    oidc.config.php: |-
      <?php
      $CONFIG = array (
        'oidc_login_client_id' => 'nc',
        'oidc_login_client_secret' => 'E4KdTMA0caH3UtAeILi5RMVm9gRz0hom',
        'oidc_login_provider_url' => 'https://sso-kc.tracexit.com/realms/nc', # Change 'master' to your realm name
        'oidc_login_logout_url' => 'https://sso-kc.tracexit.com/realms/nc/protocol/openid-connect/logout',
        'oidc_login_auto_redirect' => true,
        'oidc_login_end_session_enabled' => true,
        'oidc_login_button_text' => 'Login with Keycloak',
        'oidc_login_display_name_claim' => 'preferred_username',
      );

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: traefik
    # Traefik-specific: allow long-running connections for Nextcloud sync
    traefik.ingress.kubernetes.io/router.entrypoints: web 
  hosts:
    - host: nc.tracexit.com
      paths:
        - path: /
          pathType: Prefix