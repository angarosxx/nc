# Keep Postgres at the top
postgresql:
  enabled: true
  auth:
    database: nctracex
    username: ncusr
    password: 'DSF29jha64'

# Most reliable way to force S3 in the official chart:
nextcloud:
  host: nc.tracexit.com
  # Try using the 'externalStorage' naming convention if 'objectStore' failed
  objectStore:
    enabled: true
    config:
      region: 'us-east-1'
      bucket: 'nextcloud'
      endpoint: 'minio.minio.svc.cluster.local'
      port: 9000
      use_ssl: false
      autocreate: true
      key: 'rfibb9ivKUwQA7AxwPVD'
      secret: 'p98m97807cch4KhuhjefjJjQhy4w8wV26OWs2v8E'

  configs:
    # This manually forces S3 if the Helm chart is being stubborn
    s3.config.php: |-
      <?php
      $CONFIG = array (
        'objectstore' => array(
          'class' => '\\OC\\Files\\ObjectStore\\S3',
          'arguments' => array(
            'bucket' => 'nextcloud',
            'autocreate' => true,
            'key'    => 'rfibb9ivKUwQA7AxwPVD',
            'secret' => 'p98m97807cch4KhuhjefjJjQhy4w8wV26OWs2v8E',
            'hostname' => 'minio.minio.svc.cluster.local',
            'port' => 9000,
            'use_ssl' => false,
            'region' => 'us-east-1',
            'use_path_style'=> true
          ),
        ),
      );
    proxy.config.php: |-
      <?php
      $CONFIG = array (
        'trusted_proxies' => array('192.168.20.110'),
        'overwriteprotocol' => 'https',
      );