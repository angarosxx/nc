# 1. Disable the default internal SQLite
internalDatabase:
  enabled: false

# 2. Enable and configure the PostgreSQL sub-chart
postgresql:
  enabled: true
  auth:
    database: nctracex
    username: ncusr
    password: "DSF29jha64"
  primary:
    persistence:
      enabled: true
      storageClass: "local-path"
      size: 10Gi

# 2. Nextcloud Application Settings (MERGED)
nextcloud:
  host: nc.tracexit.com
  datadir: /var/www/html/data
  configs:
    proxy.config.php: |-
      <?php
      $CONFIG = array (
        'trusted_proxies' => array('192.168.20.110'), 
        'overwriteprotocol' => 'https',
      );

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: traefik
    # Traefik-specific: allow long-running connections for Nextcloud sync
    traefik.ingress.kubernetes.io/router.entrypoints: web 
  hosts:
    - host: nc.tracexit.com
      paths:
        - path: /
          pathType: Prefix